<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="Base.AppTheme" parent="Theme.MaterialComponents.DayNight">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/color_primary_dark</item>
        <item name="colorSecondary">?colorPrimary</item>
        <item name="colorOnSecondary">@color/base3</item>
        <item name="colorControlLight">@color/color_control_light</item>
        <item name="android:windowBackground">@color/base3</item>

        <item name="android:windowLightStatusBar" tools:targetApi="m">@bool/use_light_status</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">@bool/use_light_navigation</item>

        <item name="appBarLayoutStyle">@style/Widget.Tweetmeck.AppBar</item>
        <item name="bottomNavigationStyle">@style/Widget.Tweetmeck.BottomNav</item>
        <item name="toolbarStyle">@style/Widget.Tweetmeck.Toolbar</item>
        <item name="tabStyle">@style/Widget.Tweetmeck.Tabs</item>

        <item name="sessionListKeyline">@dimen/session_keyline</item>
        <item name="materialAlertDialogTheme">@style/AlertDialog.Theme</item>
    </style>

    <style name="AppTheme" parent="Base.AppTheme">
        <item name="android:statusBarColor">@color/base2</item>
        <item name="android:navigationBarColor">@color/base2</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.Launcher" parent="AppTheme.NoActionBar">
        <item name="android:windowBackground">@drawable/preview_window</item>
    </style>

    <style name="AppTheme.Onboarding" parent="AppTheme.NoActionBar">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>

    <style name="AppTheme.PopupTheme" parent="ThemeOverlay.MaterialComponents.Light" />

    <!-- Non-top-level screens should inflate with this theme to replace the keyline. -->
    <style name="AppTheme.Detail">
        <item name="sessionListKeyline">@dimen/margin_normal</item>
    </style>

</resources>
